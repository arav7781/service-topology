# `evidence/sources.md` shape

Generated by `build_graph_model.py --evidence-out`. This file documents the
shape so a reviewer knows what to expect; do not write the report by hand.

---

```markdown
# Topology evidence

Every edge in every generated diagram, with the location it was read from.
Generated by `build_graph_model.py`; do not edit by hand.

| | |
|---|---|
| Services | 3 |
| Topics | 2 |
| External systems | 3 |
| Edges | 10 |
| `[CODE]` | 9 |
| `[INFERENCE]` / `[UNVERIFIED]` | 1 |

---

## Confirmed edges `[CODE]`

Read directly from the cited location, or from a config key followed to a value
in a file that was also read.

| From | | To | Label | Source |
|---|---|---|---|---|
| `orders-svc` | produces&nbsp;&rarr; | `orders.created` | key=order_id | `services/orders/kafka/producer.go:20` (also `services/orders/kafka/producer.go:12`) |
| `orders.created` | &rarr;&nbsp;consumed by | `billing-svc` | group=billing | `services/billing/billing/consumers.py:12` |

---

## Inferred, not confirmed

These edges are drawn **dashed and grey**. The pattern matched, but the target
never fully resolved. Confirm each one before treating the diagram as
authoritative.

| From | | To | Label | Source | Why it is not confirmed |
|---|---|---|---|---|---|
| `billing-svc` | grpc&nbsp;&rarr; | `inventory` | InventoryService | `services/billing/billing/inventory.py:14` | gRPC stub `InventoryServiceStub` is used here, but no .proto declaring it was found in scope |

---

## Node origins

| Node | Kind | First read at |
|---|---|---|
| `billing-svc` | service | `services/billing/pyproject.toml:2` |

---

## Scan warnings

- (only present when the scan produced any)

---

## How to read the tags

| Tag | Meaning |
|---|---|
| `[CODE]` | The binding was read - a literal at the call site, or a config key followed to a concrete value in a file also read. |
| `[INFERENCE]` | The pattern matched but the target did not resolve. The reason is given for every such edge above. |
| `[UNVERIFIED]` | Asserted but not confirmed by reading a file. |
```

---

## Reviewing it

Read the "Inferred, not confirmed" table first. It is short by design, and it is
the only part of the diagram that needs a human decision. Everything in the
confirmed table can be checked by opening the cited line.
